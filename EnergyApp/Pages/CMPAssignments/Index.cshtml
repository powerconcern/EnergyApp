@page
@model EnergyApp.Pages_CMPAssignments.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CMPAssignment[0].Meter)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CMPAssignment[0].Charger)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CMPAssignment[0].Partner)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CMPAssignment) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Meter.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Charger.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Partner.ID)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
